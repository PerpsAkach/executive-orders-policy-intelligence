# Power Query lineage

Recovered project history establishes an RSS/XML path using `Xml.Tables(Web.Contents(...))`, followed by channel/item navigation, and an earlier CSV/Excel transformation path that created policy-domain fields inside Power Query.

The complete historical RSS query URL is not published here. The later Python NLP architecture superseded most classification work previously performed inside Power BI.
